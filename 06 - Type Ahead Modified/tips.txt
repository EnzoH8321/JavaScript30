JS ->

cities = [];
.json();
fetch().then().then() // The then method returns a blob of data. You must convert this data because it is in its raw form
... operator;
.filter();
new RegExp(,);
.match();
document.querySelector('.search');
.addEventListener('change', ) //The change event only fires when you off the input. For example clicking outside the element
.addEventListener('keyup', ) //Fires when a key is pressed. Indicates which key is pressed
this.value;
